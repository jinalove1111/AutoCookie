# JadeCap Automated Trading Bot - backend dependencies
# Milestone 1: foundation/infra only. Pins are major-version ranges;
# exact patch versions should be locked later via `pip freeze` once
# the app is exercised end-to-end (e.g. into a requirements.lock.txt).

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
pydantic>=2.0,<3.0
pydantic-settings>=2.2,<3.0
redis>=5.0,<6.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
