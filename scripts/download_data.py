"""download_data.py

Future purpose: pull historical candle data from the exchange (e.g. via
app.data.candle_fetcher / app.data.historical_data) and persist it into
the project's database so backtests have a local, repeatable data source.
Not implemented in Milestone 1 (infra/foundation only).
"""

if __name__ == "__main__":
    print("TODO: implement in later milestone")
